PORT=3001
# SPA (ör. Vite): http://localhost:5173 — virgülle birden fazla. Boş veya * = tüm origin’lere izin (geliştirme).
CORS_ORIGIN=http://localhost:5173
DATABASE_URL=postgresql://<USER>:<PASSWORD>@<HOST>:<PORT>/<DATABASE>
UPLOAD_DIR=uploads
OUTPUT_DIR=outputs
PIPELINE_ENTRYPOINT=../pipeline/main.py
SEG_MODEL_PATH=../pipeline/models/segmentation.pth
DMG_MODEL_PATH=../pipeline/models/damage.pth
PYTHON_BIN=python
API_KEY=change-me
GEOTIFF_VALIDATOR_PATH=./scripts/validate_geotiff.py
